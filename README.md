# test_game

Шаблон проекта на базе **React + Tailwind + Zustand** (Vite + TypeScript).

## Запуск

```bash
npm i
npm run dev
```

## Скрипты

- `npm run dev` — dev server
- `npm run build` — production build
- `npm run preview` — предпросмотр production build